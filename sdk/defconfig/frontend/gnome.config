# Frontend: GNOME (Wayland, mutter/gnome-shell), for desktop x86_64/arm64
# machines - the systemd-free arrangement Gentoo and Alpine ship: elogind
# provides org.freedesktop.login1 (D-Bus activated), gnome-session runs
# its builtin startup, and the session is registered through pam_elogind
# at login (see the overlay's /etc/pam.d/login).
#
# Include this AFTER the image profile (i.e. from a board.config): the
# negations below unwind the default EmulationStation/sway frontend, which
# works because board fragments are emitted last and later assignments win.
#
# STATUS: all five stages packaged and kconfig-validated; nothing compiled
# or booted yet (see frontend/README.md). mozjs is the expected trouble
# spot on first build.

# No sway session. Xwayland STAYS: mutter runs it for X11 apps.
#
# The game stack is NOT negated: EmulationStation, RetroArch and the cores
# are built and installed, they are just not what the machine boots into.
# Nothing starts them here - the frontend is launched from the sway
# session's config, and this image has no sway - so ES is an application
# among others, started from the desktop (see the .desktop entry in the
# common overlay) when the user wants it.
# BR2_PACKAGE_SWAYWM is not set
# BR2_PACKAGE_SWAYWM_SWAYBAR is not set
# BR2_PACKAGE_FOOT is not set
# BR2_PACKAGE_WLROOTS_XWAYLAND is not set
BR2_PACKAGE_XWAYLAND=y
BR2_PACKAGE_XAPP_XKBCOMP=y

# The shell drives the whole stack through GObject introspection typelibs.
BR2_PACKAGE_GOBJECT_INTROSPECTION=y

# Session registration: PAM with pam_elogind (elogind builds its module
# when linux-pam is present), and util-linux login - busybox login speaks
# no PAM.
BR2_PACKAGE_LINUX_PAM=y
BR2_PACKAGE_UTIL_LINUX=y
BR2_PACKAGE_UTIL_LINUX_LOGIN=y

# libpulse for gnome-settings-daemon's volume control; the server side is
# pipewire-pulse, which the image already runs - the PulseAudio daemon
# itself stays off.
BR2_PACKAGE_PULSEAUDIO=y

# GTK 4 (Wayland + X11 for Xwayland clients) and the GNOME platform
# libraries.
BR2_PACKAGE_LIBGTK3=y
BR2_PACKAGE_LIBGTK3_X11=y
BR2_PACKAGE_LIBGTK4=y
BR2_PACKAGE_LIBGTK4_WAYLAND=y
BR2_PACKAGE_LIBGTK4_X11=y
BR2_PACKAGE_LIBADWAITA=y
BR2_PACKAGE_LIBRSVG=y
BR2_PACKAGE_SHARED_MIME_INFO=y
BR2_PACKAGE_HICOLOR_ICON_THEME=y

# The stack itself; gnome-shell selects mutter's and its own runtime set
# (gnome-session, gnome-settings-daemon, e-d-s, ibus, gcr4...).
BR2_PACKAGE_ELOGIND=y
BR2_PACKAGE_DCONF=y
BR2_PACKAGE_LIBNOTIFY=y
BR2_PACKAGE_MOZJS128=y
BR2_PACKAGE_GJS=y
BR2_PACKAGE_MUTTER=y
# libgdm's Gdm-1.0 typelib, which gnome-shell's JavaScript imports at startup
# whether or not a display manager is used. Without it the shell aborts after
# mutter has already come up:
#
#   JS ERROR: Error: Requiring Gdm, version 1.0: Typelib file for namespace
#     'Gdm', version '1.0' not found
#
# Nothing starts the gdm daemon; this image autologins through agetty.
# The wallpapers gsettings-desktop-schemas already points at by absolute path.
# Without them the shell finds nothing at file:///usr/share/backgrounds/gnome/
# and draws a flat colour instead - silently, with nothing logged.
BR2_PACKAGE_GNOME_BACKGROUNDS=y
BR2_PACKAGE_GDM=y
BR2_PACKAGE_GNOME_SHELL=y
BR2_PACKAGE_GNOME_SESSION=y
BR2_PACKAGE_GNOME_SETTINGS_DAEMON=y

# Network: nothing to switch any more. NetworkManager is what every image
# runs (see network.config), which is what the shell's network indicator
# and the Settings network panel expect to drive.

# Core applications. Web needs webkitgtk (built in its GTK 4 mode since
# libgtk4 is in this config); Settings drags the deepest tree - cups,
# NetworkManager/libnma, ModemManager, accountsservice, online-accounts,
# udisks; the network stack switch above keeps it consistent. The Image Viewer (Loupe) and Camera
# (Snapshot) are Rust applications and deliberately not packaged yet.
BR2_PACKAGE_WEBKITGTK=y
BR2_PACKAGE_EPIPHANY=y
BR2_PACKAGE_GNOME_CONTROL_CENTER=y
BR2_PACKAGE_NAUTILUS=y
BR2_PACKAGE_GNOME_CALCULATOR=y
BR2_PACKAGE_GNOME_CALENDAR=y
BR2_PACKAGE_GNOME_CONSOLE=y
BR2_PACKAGE_GNOME_DISK_UTILITY=y
# Decibels is written in TypeScript, so it needs tsc during the build. That
# comes from host-typescript in ports, which runs on host-nodejs - a virtual
# package, so a provider has to be named here. The pre-built variant is the
# default on an x86_64 build machine and is what this selects; nothing of node
# reaches the target, only the JavaScript tsc emits.
BR2_PACKAGE_HOST_NODEJS=y
# Its interface is Blueprint rather than raw GtkBuilder XML, so nine .blp files
# are compiled to .ui during the build. Decibels looks the compiler up with
# find_program and otherwise falls back to a wrap subproject cloned from
# gitlab, which --wrap-mode=nodownload refuses - so without this the configure
# step stops rather than quietly building something lesser. Named here rather
# than left to decibels' dependency list, so the tool is part of what this
# frontend declares it needs.
BR2_PACKAGE_HOST_BLUEPRINT_COMPILER=y
BR2_PACKAGE_DECIBELS=y
BR2_PACKAGE_SHOWTIME=y
BR2_PACKAGE_GST1_PLUGINS_BASE=y
# Screen recording and screen sharing. gnome-shell runs its recorder as a
# D-Bus-activated service (org.gnome.Shell.Screencast) whose pipeline is
# pipewiresrc ! videoconvert ! vp8enc ! webmmux, so without these elements the
# service cannot start and the shell reports it at every login:
#
#   GNOME Shell-Message: Error connecting to the screencast service
#
# pipewiresrc comes from PipeWire's own GStreamer plugin, vp8enc and webmmux
# from gst1-plugins-good, and appsrc/appsink from gst1-plugins-base - which
# the portal's screencast path uses too.
BR2_PACKAGE_PIPEWIRE_GSTREAMER=y
BR2_PACKAGE_GST1_PLUGINS_GOOD=y
BR2_PACKAGE_GST1_PLUGINS_GOOD_PLUGIN_VPX=y
BR2_PACKAGE_GST1_PLUGINS_GOOD_PLUGIN_MATROSKA=y
BR2_PACKAGE_GST1_PLUGINS_BASE_PLUGIN_APP=y

# Portals: the GNOME backend plus the GTK3 fallback.
BR2_PACKAGE_XDG_DESKTOP_PORTAL=y
BR2_PACKAGE_XDG_DESKTOP_PORTAL_GNOME=y
BR2_PACKAGE_XDG_DESKTOP_PORTAL_GTK=y

# evolution-data-server's need for libical's GLib bindings is handled:
# Buildroot's libical does hard-code ICAL_GLIB=OFF, so ports/external.mk turns
# it back on and declares the host libical whose exported generator a cross
# build of libical-glib requires.

# Application managers: Flatpak + GNOME Software, Lutris, Heroic (see the
# fragment for what runs where). Desktop images are where an app store
# makes sense; the game-frontend images get content via ES/PortMaster.
include sdk/defconfig/appmanager.config

# KNOWN GAP: spellcheck does not work in this image and cannot be switched
# on from a fragment. enchant is here already - webkitgtk selects it - but
# enchant is only a dispatcher: it looks a provider up at runtime and does
# nothing whatever when none was built. Buildroot packages no provider at
# all: there is no hunspell, no nuspell and no aspell in package/, so text
# entry in the browser and in GTK apps silently is not checked.
#
# Closing it means packaging a backend the way ports does xar and glslang -
# hunspell being the one GTK and WebKit expect - and then giving enchant an
# edge to it. That second half matters: Buildroot's enchant.mk declares only
# libglib2 and host-pkgconf while enchant's configure autodetects providers
# through pkg-config, so even with hunspell built, whether enchant finds it
# comes down to the order make walks two packages with no dependency between
# them, which under -j is not deterministic. So: a ports package plus
# ENCHANT_DEPENDENCIES += hunspell, not a defconfig line.
#
# Writing BR2_PACKAGE_HUNSPELL=y here does nothing at all - kconfig drops an
# unknown symbol without a word, and the build looks fine.

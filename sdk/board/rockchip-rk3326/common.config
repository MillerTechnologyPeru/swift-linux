# Shared board options for Rockchip RK3326 (quad Cortex-A35, Mali-G31) handhelds.
#
# A device board.config includes this and adds its device tree name (and,
# where U-Boot applies, its U-Boot defconfig):
#
#     include sdk/board/rockchip-rk3326/common.config
#     BR2_LINUX_KERNEL_INTREE_DTS_NAME="..."
#
# DEFINITION ONLY - not built or verified. A real build needs a kernel with
# these devices' DTS and driver patches (see patches/README.md).

BR2_aarch64=y
BR2_cortex_a35=y

BR2_LINUX_KERNEL_DTS_SUPPORT=y
BR2_LINUX_KERNEL_CONFIG_FRAGMENT_FILES="@SWIFT_LINUX@/sdk/board/rockchip-rk3326/linux.fragment"
BR2_LINUX_KERNEL_CUSTOM_DTS_PATH="@SWIFT_LINUX@/sdk/board/rockchip-rk3326/dts"
BR2_GLOBAL_PATCH_DIR="@SWIFT_LINUX@/sdk/board/rockchip-rk3326/patches @BOARD@/patches"

include sdk/defconfig/gpu/panfrost.config

BR2_TARGET_GENERIC_GETTY_PORT="tty0"

# Boot: U-Boot + extlinux from SD/eMMC (device board.config sets the U-Boot
# defconfig).
# BL31 comes from a vendor rkbin blob (rk3326), not Buildroot's ATF.
BR2_TARGET_UBOOT=y
BR2_TARGET_UBOOT_BUILD_SYSTEM_KCONFIG=y
BR2_TARGET_UBOOT_NEEDS_DTC=y
BR2_TARGET_UBOOT_NEEDS_PYLIBFDT=y
BR2_TARGET_UBOOT_FORMAT_CUSTOM=y
BR2_TARGET_UBOOT_FORMAT_CUSTOM_NAME="u-boot-rockchip.bin"

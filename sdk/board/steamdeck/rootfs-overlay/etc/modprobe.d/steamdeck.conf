# Keep the built-in controller in gamepad mode. Without this the firmware
# stays in "lizard mode", where the sticks and pads emulate a keyboard and
# mouse instead of presenting a gamepad to evdev.
options hid_steam lizard_mode=0

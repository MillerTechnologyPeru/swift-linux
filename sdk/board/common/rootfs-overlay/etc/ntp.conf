# ntpd configuration for Swift Linux.
#
# The rootfs is read-only, so the drift file goes on the /tmp tmpfs.
driftfile /tmp/ntp.drift

# Public NTP pool.
pool pool.ntp.org iburst

# Sensible default restrictions.
restrict default nomodify nopeer noquery limited kod
restrict 127.0.0.1
restrict ::1

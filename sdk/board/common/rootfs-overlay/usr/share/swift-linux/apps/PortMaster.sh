#!/bin/sh
# Open PortMaster (seeded into /data/apps by S15data on images that ship
# it). The real work - first-run expansion onto the data partition - is
# in /usr/bin/portmaster.
exec /usr/bin/portmaster

# Shared board options for Amlogic S922X / G12B (4x A73 + 2x A53, Mali-G52) handhelds.
#
# A device board.config includes this and adds its device tree name (and,
# where U-Boot applies, its U-Boot defconfig):
#
#     include sdk/board/amlogic-s922x/common.config
#     BR2_LINUX_KERNEL_INTREE_DTS_NAME="..."
#
# DEFINITION ONLY - not built or verified. A real build needs a kernel with
# these devices' DTS and driver patches (see patches/README.md).

BR2_aarch64=y
BR2_cortex_a73_a53=y

BR2_LINUX_KERNEL_DTS_SUPPORT=y
BR2_LINUX_KERNEL_CONFIG_FRAGMENT_FILES="@SWIFT_LINUX@/sdk/board/amlogic-s922x/linux.fragment"
BR2_LINUX_KERNEL_CUSTOM_DTS_DIR="@SWIFT_LINUX@/sdk/board/amlogic-s922x/dts"
BR2_GLOBAL_PATCH_DIR="@SWIFT_LINUX@/external/patches/packages @SWIFT_LINUX@/sdk/board/amlogic-s922x/patches @BOARD@/patches"

include sdk/defconfig/gpu/panfrost.config

BR2_TARGET_GENERIC_GETTY_PORT="tty0"

# Boot: U-Boot + extlinux from SD/eMMC (device board.config sets the U-Boot
# defconfig).
# Amlogic boot needs the vendor FIP signing chain on top of U-Boot.
BR2_TARGET_UBOOT=y
BR2_TARGET_UBOOT_BUILD_SYSTEM_KCONFIG=y
BR2_TARGET_UBOOT_NEEDS_DTC=y
BR2_TARGET_UBOOT_NEEDS_PYLIBFDT=y
BR2_TARGET_UBOOT_FORMAT_CUSTOM=y
BR2_TARGET_UBOOT_FORMAT_CUSTOM_NAME="u-boot.bin"

# Radio/SoC blobs the mainline linux-firmware set does not carry.
BR2_PACKAGE_FIRMWARE_ARMBIAN=y

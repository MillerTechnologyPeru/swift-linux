config BR2_PACKAGE_LIBGUSB
	bool "libgusb"
	depends on BR2_USE_MMU
	select BR2_PACKAGE_LIBUSB
	select BR2_PACKAGE_JSON_GLIB
	help
	  GObject wrapper around libusb; colord's hardware sensors go
	  through it.

	  https://github.com/hughsie/libgusb
